flask>=3.0.0
soundfile>=0.12.0
numpy<2
pyttsx3>=2.90
# TTS installed separately via install_deps.bat (needs --prefer-binary on Windows)
# torch installed separately via install_deps.bat with CUDA index URL
